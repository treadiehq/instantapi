name = "instant-api-sandbox"
main = "src/index.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Sandbox binding
# Configure via Dashboard: Workers & Pages → instant-api-sandbox → Settings → Variables → Add Binding

